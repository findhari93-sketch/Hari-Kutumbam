import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.harikutumbam.expense',
  appName: 'Expense Tracker Pro',
  webDir: 'out'
};

export default config;
